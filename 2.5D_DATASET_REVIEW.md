# 2.5D 数据集处理逻辑检查报告

## 📋 检查范围

全面检查代码库中对 2.5D 数据集的处理逻辑，包括：
- 数据集加载 (`dataset.py`)
- 训练流程 (`worker.py`)
- 测试流程 (`worker.py`)
- 模型构建 (`worker.py`, `models.py`)
- GUI 界面 (`main.py`)
- 配置管理 (`config.py`)

---

## ✅ 已正确实现的部分

### 1. 数据集加载 (`dataset.py`)

**位置：** `dataset.py` 第 35-373 行

**实现状态：** ✅ 完整且正确

**关键特性：**
- ✅ 支持递归搜索子文件夹中的 `.tif` 文件
- ✅ 正确解析文件名格式：`TCGA_CS_5393_19990606_1.tif` → `(case_id, slice_id)`
- ✅ 2.5D 堆叠逻辑：Channel 0=上一张，Channel 1=当前，Channel 2=下一张
- ✅ 边界处理：如果相邻切片不存在，复制当前帧
- ✅ 维度验证：确保返回 `(3, H, W)` 的图像和 `(1, H, W)` 的掩码
- ✅ 修复双重归一化 Bug：不再手动除以 255.0，让 albumentations 处理

**代码片段：**
```python
# 堆叠为 3 通道 (H, W, 3)
stacked_img = np.dstack([prev_img, current_img, next_img])
```

---

### 2. 训练流程中的 2.5D 处理 (`worker.py`)

#### 2.1 数据划分逻辑

**位置：** `worker.py` 第 3188-3233 行

**实现状态：** ✅ 正确

**关键特性：**
- ✅ 使用 `GroupShuffleSplit` 按 `case_id` 分组划分，避免数据泄露
- ✅ 正确存储 `train_indices` 和 `val_indices` 用于后续创建子集
- ✅ 验证分组正确性，检测并报告数据泄露

**代码片段：**
```python
# 按 case_id 分组划分
gss = GroupShuffleSplit(n_splits=1, test_size=0.3, random_state=42)
train_idx, val_idx = next(gss.split(all_indices, groups=groups))
```

#### 2.2 数据集加载 (`load_dataset`)

**位置：** `worker.py` 第 5018-5083 行

**实现状态：** ✅ 正确

**关键特性：**
- ✅ 检测 `dataset_type == "2.5d"` 并创建 `TCGA2_5DDataset`
- ✅ 支持递归搜索子文件夹
- ✅ 正确传递 `transform` 和 `is_train` 参数

**代码片段：**
```python
if self.dataset_type == "2.5d" and TCGA2_5D_AVAILABLE:
    base_dataset = TCGA2_5DDataset(
        data_dir=self.data_dir,
        mask_dir=mask_dir,
        transform=transform,
        is_train=(split_name == "train"),
        debug=False
    )
```

#### 2.3 训练/验证数据加载器创建

**位置：** `worker.py` 第 3305-3347 行

**实现状态：** ✅ 正确

**关键特性：**
- ✅ 2.5D 数据集使用索引创建 `Subset`
- ✅ 标准数据集使用 `patient_ids` 创建数据集
- ✅ 正确区分两种数据集类型的加载方式

---

### 3. 模型构建中的 2.5D 支持

#### 3.1 SMP U-Net++

**位置：** `worker.py` 第 5197-5228 行

**实现状态：** ✅ 已修复

**关键特性：**
- ✅ 根据 `dataset_type` 动态设置 `in_channels`：
  - `2.5d` → `in_channels = 3`
  - `standard` → `in_channels = 1`

#### 3.2 DeepLabV3+

**位置：** `worker.py` 第 5209-5235 行

**实现状态：** ✅ 已修复

**关键特性：**
- ✅ 根据 `dataset_type` 动态设置 `in_channels`
- ✅ 与 SMP U-Net++ 使用相同的逻辑

#### 3.3 Checkpoint 保存和恢复

**位置：** `worker.py` 第 5485-5516 行（`_extract_model_config`）

**实现状态：** ✅ 已修复

**关键特性：**
- ✅ 为 SMP 模型保存 `in_channels` 和 `dataset_type`
- ✅ 从模型第一层卷积推断 `in_channels`
- ✅ 测试时从 checkpoint 恢复配置

---

## ⚠️ 发现的问题

### 问题 1：测试模块不支持 2.5D 数据集

**位置：** `worker.py` 第 165-214 行（`ModelTestThread.run`）

**问题描述：**
- 测试模块假设数据目录有 `patient_ids` 子文件夹结构
- 对于 2.5D 数据集，目录结构是扁平的（所有 `.tif` 文件在同一目录或子目录）
- 当前代码会抛出 `ValueError: 测试数据目录为空，未找到子文件夹`

**错误代码：**
```python
# 第 176-180 行
patient_ids = [pid for pid in os.listdir(self.data_dir) 
              if os.path.isdir(os.path.join(self.data_dir, pid))]

if not patient_ids:
    raise ValueError("测试数据目录为空，未找到子文件夹")
```

**影响：**
- 无法使用测试模块测试 2.5D 数据集训练的模型

**建议修复：**
- 在 `ModelTestThread` 中添加 `dataset_type` 参数
- 根据 `dataset_type` 选择不同的数据加载逻辑
- 2.5D 数据集直接使用 `TCGA2_5DDataset`，无需 `patient_ids`

---

### 问题 2：测试模块未传递 `dataset_type`

**位置：** `worker.py` 第 166-172 行

**问题描述：**
- 创建临时 `TrainThread` 时未传递 `dataset_type`
- 临时实例会使用默认值 `"standard"`，导致 2.5D 数据集无法正确加载

**当前代码：**
```python
temp_train_thread = TrainThread(
    data_dir=self.data_dir,
    epochs=1,
    batch_size=4,
    model_path=None,
    save_best=False
    # ⚠️ 缺少 dataset_type 参数
)
```

**影响：**
- 即使修复了问题 1，测试模块仍无法正确加载 2.5D 数据集

---

### 问题 3：其他模型未实现动态通道数设置

**位置：** `worker.py` 第 5123-5237 行（`_build_model`）

**问题描述：**
- `ImprovedUNet`、`ResNetUNet`、`TransUNet`、`DS-TransUNet`、`SwinUNet` 等模型
- 这些模型的 `in_channels` 固定为 3，未根据 `dataset_type` 动态设置

**当前代码：**
```python
# DS-TransUNet (第 5132 行)
"in_channels": 3,  # 固定为 3

# SwinUNet (第 5150 行)
"in_channels": 3,  # 固定为 3
```

**影响：**
- 如果使用标准数据集（1通道）训练这些模型，会导致维度不匹配错误
- 如果使用 2.5D 数据集（3通道）训练，可以正常工作

**建议修复：**
- 为所有模型添加动态 `in_channels` 设置逻辑
- 或至少添加检查，确保 `in_channels` 与数据集类型匹配

---

### 问题 4：数据增强配置未区分数据集类型

**位置：** `worker.py` 第 3249-3284 行

**问题描述：**
- 训练和验证的数据增强配置对标准数据集和 2.5D 数据集使用相同的配置
- 2.5D 数据集已经是 3 通道（堆叠后的），归一化使用 ImageNet 的 3 通道均值/标准差是正确的
- 但标准数据集是 1 通道，使用 3 通道的归一化参数可能不合适

**当前代码：**
```python
A.Normalize(mean=(0.485, 0.456, 0.406), std=(0.229, 0.224, 0.225)),  # 3通道归一化
```

**影响：**
- 对于标准数据集（1通道），归一化参数不匹配（虽然可能不会报错，但可能影响性能）

**建议修复：**
- 根据 `dataset_type` 动态设置归一化参数：
  - 2.5D 数据集：`mean=(0.485, 0.456, 0.406), std=(0.229, 0.224, 0.225)`
  - 标准数据集：`mean=(0.5,), std=(0.5,)` 或单通道 ImageNet 归一化

---

## 📊 总结

### 已正确实现 ✅

1. ✅ 数据集加载逻辑（`dataset.py`）
2. ✅ 训练流程中的 2.5D 处理（数据划分、数据集创建）
3. ✅ SMP 模型（U-Net++ 和 DeepLabV3+）的动态通道数设置
4. ✅ Checkpoint 保存和恢复配置
5. ✅ GUI 界面已解除强制配对限制

### 需要修复 ⚠️

1. ⚠️ **测试模块不支持 2.5D 数据集**（高优先级）
2. ⚠️ **测试模块未传递 `dataset_type`**（高优先级）
3. ⚠️ **其他模型未实现动态通道数设置**（中优先级）
4. ⚠️ **数据增强归一化参数未区分数据集类型**（低优先级）

---

## 🔧 修复建议

### 优先级 1：修复测试模块支持 2.5D

1. 在 `ModelTestThread.__init__` 中添加 `dataset_type` 参数
2. 修改 `ModelTestThread.run` 中的数据加载逻辑，支持 2.5D 数据集
3. 在创建临时 `TrainThread` 时传递 `dataset_type`

### 优先级 2：统一所有模型的动态通道数设置

1. 为所有模型添加根据 `dataset_type` 动态设置 `in_channels` 的逻辑
2. 确保标准数据集使用 1 通道，2.5D 数据集使用 3 通道

### 优先级 3：优化数据增强配置

1. 根据 `dataset_type` 动态设置归一化参数
2. 确保归一化参数与输入通道数匹配

---

## 📝 代码位置索引

| 功能 | 文件 | 行号 |
|------|------|------|
| 2.5D 数据集类 | `dataset.py` | 35-373 |
| 训练数据划分 | `worker.py` | 3188-3233 |
| 数据集加载 | `worker.py` | 5018-5083 |
| SMP U-Net++ 构建 | `worker.py` | 5197-5228 |
| DeepLabV3+ 构建 | `worker.py` | 5209-5235 |
| Checkpoint 保存 | `worker.py` | 5485-5516 |
| 测试模块（需修复） | `worker.py` | 165-214 |

